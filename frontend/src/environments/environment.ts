// export const environment = {
//   production: false,
//   apiUrl: 'http://localhost:3000',
// };

export const environment = {
  production: false,
  apiUrl: '/api',  // Proxied through Angular dev server (HTTPS → HTTPS backend)
};